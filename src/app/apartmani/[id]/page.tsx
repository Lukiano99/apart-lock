import { SearchbarProps } from "@/layouts/components/searchbar";

const ApartmanId = ({ params }: { params: SearchbarProps }) => {
  return <div>@TODO - Apartman {params.id}</div>;
};

export default ApartmanId;
